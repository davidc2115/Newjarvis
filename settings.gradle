pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // JitPack — requis pour la lib llama-android (binding llama.cpp)
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "JarvisAssistant"
include ':app'
include ':sdcpp'
