<?xml version="1.0" encoding="utf-8"?>
<!-- Bouton d'action principal réutilisé partout dans l'app (CTA, micro, envoi...) :
     pilule à dégradé cyan → violet néon, avec ondulation tactile (ripple) au toucher
     pour un retour visuel immédiat — plus "high-tech", plus intuitif pour tout le monde. -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="#66FFFFFF">
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:type="linear"
                android:angle="45"
                android:startColor="@color/cyan_accent"
                android:centerColor="@color/electric_blue"
                android:endColor="@color/violet_accent" />
            <corners android:radius="16dp" />
        </shape>
    </item>
</ripple>
